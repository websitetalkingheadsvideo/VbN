# Non‑Admin A11y Sweep — To‑Do (2025‑11‑10)

## Public Pages
- Register
  - Add `role="alert" aria-live="polite"` to error/success messages.
  - Ensure first invalid field is focused on submit failure (Bootstrap + helper).
  - Add helpful `autocomplete` attributes (username, email, new-password, current-password).
- Login
  - Keep as a standalone page (no shared header/footer).
  - Add `role="alert" aria-live="polite"` to disabled-login and error messages.
- Chat
  - Toggle `aria-busy` on `#characterList` from true → false after data loads.
  - Announce character count changes via `aria-live="polite"`.
  - Ensure selected character reveal is announced; keep visible focus styles.
// Legacy alternate questionnaire (cc.php) has been removed to avoid duplication. No action required.

## Global Landmarks / ARIA
- Confirm `<main id="main-content" role="main">` via shared header on public pages (index, chat, questionnaire, 404/500 already using header).
- Add `<nav aria-label="Primary Navigation">` where any page includes a nav block.

## Async Feedback
- Add `aria-live="polite"` to other async areas (e.g., character loads, uploads) and toggle `aria-busy` appropriately.

## Modal Support (Public)
- Add lightweight modal focus‑trap helper (ESC close, inert background, focus return) for any non‑admin modals.
- Ensure the modal container is focusable (focus the container first) and first focusable element is visible.

## Validation / Focus
- Ensure first invalid field gets focus after failed submit on public forms.
- Keep subtle focus styles visible for keyboard users across pages.
